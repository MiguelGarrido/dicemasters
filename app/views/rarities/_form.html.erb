<%= form_for @rarity do |f| %>
  <% if @rarity.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@rarity.errors.count, "error") %> prohibited
      this rarity from being saved:</h2>
    <ul>
    <% @rarity.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>  
 
  <p>
    <%= f.submit %>
  </p>  
<% end %>
